services:
  smart-gateway:
    build: .
    container_name: smart-gateway
    restart: unless-stopped
    ports:
    - "8765:8765"
    env_file:
      - .env
    environment:
      LOG_DIR: /app/logs
    volumes:
      - ./logs:/app/logs
